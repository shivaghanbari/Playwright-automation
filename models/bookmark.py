class Bookmark:
    def __int__(self, page):
        self.page = page

    # user can find product in two ways,directly and un-directly.first method attempts to find product by searching in search bar
    # and in second one, user tries to reach it by scrolling and click on brands etc.
    def find_product_by_search_bar(self):




    def find_product_in_brand_list(self):


